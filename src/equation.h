#include <iostream>
#include <cstring>
#include <string>
/// @brief check if the string is available for string actions
/// @param prompt 
/// @return true / false

bool equation_available(std::string prompt) {   
    
}