#include "date.h"
#include "iostream"
#include "deftypes.h"
#include "filesystem"
using namespace std;
/*
TODO:
recordLog(date actionDate, char * fileExecuted, bool error, char * errorArgcp[])
actionDate is from "date.h", provides the time for the current action
*/
/// @brief 
void recordLog(date actionDate, char * fileExecuted, bool error, char * errorArgcp[]);